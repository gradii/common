// Configuration for IDEs only.
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "..",
    "baseUrl": ".",
    "paths": {
      "@gradii/annotation": [
        "../annotation"
      ]
    }
  },
  "include": [
    "./**/*.ts"
  ],
  "exclude": ["**/*.spec.ts"]
}